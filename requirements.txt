# Video Subtitle Generator - Requirements
# Clean, minimal dependencies for production deployment

# Core AI & Processing
faster-whisper>=1.2.1
torch>=2.12.1
customtkinter>=5.2.2
moviepy>=2.2.1

# Media Processing
imageio-ffmpeg>=0.5.1

# Note: The following are automatically installed as dependencies:
# - numpy (faster-whisper dependency)
# - av (faster-whisper dependency)
# - onnxruntime (VAD/Silero model)
# - Pillow (CustomTkinter dependency)
# - tqdm (progress bars)

# GPU Acceleration (Optional)
# For NVIDIA GPU support, install CUDA-enabled PyTorch:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Installation:
# pip install -r requirements.txt


